Queries are written directly in the controllers with parameterized
`mysql2` calls (see `server/config/db.js`). This folder is a
placeholder for a future ORM/model layer (e.g. Sequelize or Knex) if
the project grows; it isn't required for the current feature set.
